def findmax(folder):
    # 假设folder里有 subfolders = [] 和 files = []
    if subfolder is empty:
        return max of all files
    largest files in subfolders = []
    for f in subfolders:
        largest files in subfolders append findmax(f)
    return max of ( max of largest files in subfolders 和 max of files)
